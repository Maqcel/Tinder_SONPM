import 'package:flutter/material.dart';
import 'package:tinder/domain/model/possible_match/possible_match.dart';
import 'package:tinder/extensions/build_context_extension.dart';
import 'package:tinder/gen/assets.gen.dart';

class TopPicksScreen extends StatefulWidget {
  const TopPicksScreen({Key? key}) : super(key: key);

  @override
  _TopPicksScreenState createState() => _TopPicksScreenState();
}

class _TopPicksScreenState extends State<TopPicksScreen> {
  // widget._matchList;

  @override
  Widget build(BuildContext context) => Text("one");
      // Container(child: Stack(
      //   alignment: Alignment.bottomCenter, children: [_body(), _textCard()],))


  // TODO: delete this file


}